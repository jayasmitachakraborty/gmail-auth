google-auth-httplib2 
google-auth>=2.29.0,<3
google-auth-oauthlib>=1.2.0,<2      # only needed locally for token refresh
google-api-python-client>=2.126.0,<3
google-cloud-bigquery>=3.19.0,<4
pydantic>=2.7.0,<3
pydantic-settings>=2.2.0,<3

# Cloud Functions v2 HTTP runtime requires functions-framework which bundles Flask.
# List it explicitly so local installs and the GCF build step are consistent.
functions-framework>=3.5.0,<4